# Stack

A simple generic Stack implementation in Go.

A stack is a linear data structure that follows the **LIFO** principle:

> Last In, First Out

This means the last element added to the stack is the first one removed.

---

## Features

- Generic stack implementation
- Supports any data type
- Push elements
- Pop elements
- Peek at the top element
- Check if stack is empty
- Check stack size
- Supports unlimited stack
- Supports fixed-size stack

---

## Installation

```bash
go get github.com/divijsehgal25/go-ds
